server:
  port: 8080
spring:
  profiles:
    active:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver
    platform: mysql
    url: jdbc:p6spy:mysql://47.107.171.232:3306/vue_freestyle?serverTimezone=UTC&useUnicode=true&charaterEncoding=utf-8&useSSL=false
    username: root
    password: Root123!@#
    initialSize: 5
    minIdle: 5
    maxActive: 20
    maxWait: 60000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1 FROMDUAL
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    #filters: stat,wall,log4j
    logSlowSql: true

  # redis
  redis:
    database: 0
    host: 127.0.0.1
    password: ''
    port: 6379
    timeout: 10000ms
    lettuce:
      pool:
        max-idle: 8
        max-wait: 2000ms
        min-idle: 0

# mybatis plus
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath*:/mapper/*.xml
  global-config:
    db-config:
      logic-not-delete-value: 1
      logic-delete-value: 2


vuefreestyle:
  swagger:
    enabled: true

